{% extends '::base.html.twig' %}
{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('bundles/cinemax/css/catalog.css') }}" type="text/css" rel="stylesheet">
    <link href="{{ asset('bundles/cinemax/css/rcarousel_catalog.css') }}" type="text/css" rel="stylesheet">
{% endblock %}
{% block javascript %}
    <script src="{{ asset('bundles/cinemax/js/ajax_sort.js') }}" type="text/javascript" ></script>
{% endblock %}
{% block body %}
    <a name="catalog"></a>
    <div class="all_catalog">
        <span>{{ title }}</span>
        <div style="position: relative;">
        <div class="sort_header">Сортировка по типам</div>
        <ul class="layout_sort">
            <li class="float_left"><a href="{{ path('cinemax_sort_by_type', {'type':'Film'}) }}#catalog">Фильм&nbsp</a></li>
            <li class="float_left"><a href="{{ path('cinemax_sort_by_type', {'type':'Multfilm'}) }}#catalog">Мультфильм&nbsp</a></li>
            <li class="float_left"><a href="{{ path('cinemax_sort_by_type', {'type':'Clip'}) }}#catalog">Клип&nbsp</a></li>
            <li class="float_left"><a href="{{ path('cinemax_sort_by_type', {'type':'Serial'}) }}#catalog">Сериал&nbsp</a></li>
            <li class="float_left"><a href="{{ path('cinemax_sort_by_type', {'type':'Multserial'}) }}#catalog">Мультсериал</a></li>
        </ul>
        </div>
        <div class="catalog_to_bin"><a href="{{ path('cinemax_bin_discs')}}#bin"><p>Перейти в корзину</p></a></div>
        <div class="all_catalog_inner_border">
            {% for disc in pagination %}
                {% include  "CinemaxBundle:content:item.html.twig" with { 'disc': disc } %}
            {% endfor %}
        </div>
        <div class="paginate">
            {{ knp_pagination_render(pagination) }}
        </div>
    </div>
{% endblock %}